{
  "name": "claro-automation",
  "version": "2.0.0",
  "description": "This project is for validate all SG VENTAS Project ",
  "main": "index.js",
  "scripts": {
    "cypress:runner": "cypress open",
    "cypress:execution": "cypress run",
    "cypress:regression:test": "cypress run --env tags=@ejecutar",
    "generate:report:txt": "node ./reportsTXT/ReporteTXT.js"
  },
  "keywords": [
    "cypress"
  ],
  "author": "cfotech",
  "license": "ISC",
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "latest",
    "@bahmutov/cypress-esbuild-preprocessor": "latest",
    "cypress": "^12.17.4",
    "cypress-plugin-api": "^2.11.0",
    "esbuild": "latest",
    "multiple-cucumber-html-reporter": "latest",
    "oracledb": "latest"
  },
  "@badeball/cypress-cucumber-preprocessor": {
    "json": {
      "enabled": true
    }
  }
}
